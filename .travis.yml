---
language: node_js
node_js:
  - "6"

sudo: true
dist: trusty

env:
  - CXX=g++-4.8

addons:
  chrome: stable
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - xvfb
      - g++-4.8

cache:
  yarn: true
  directories:
    - $HOME/.npm
    - node_modules

stages:
  - name: after_success
    # only want to deploy if master build
    if: branch = master

before_install:
  - npm config set spin false

install:
  - npm install
  - export DISPLAY=':99.0'
  - Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &

before_script:
  - npm install -g bower
  - npm install -g coveralls
  - npm install -g firebase-tools
  - bower install

after_success:
  - cat coverage/lcov.info | coveralls
  - ember build
  - ember electron:make --out public/assets/installers --platform linux --prune --arch all --app-version 1.0.0
  - firebase deploy --token "$FIREBASE_TOKEN"
